# Core (cross-platform)
tkhtmlview==1.4.8
tkcalendar==1.6.1
ttkbootstrap==1.6.3
win10toast==0.9  # only useful on Windows; import safely handled in code

# Outlook integration (Windows only)
pywin32==305

# Utilities
pytz==2024.1
